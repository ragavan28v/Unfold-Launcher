package com.ragavan.unfold.ui.components.glass

import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp

object GlassStyle {

    val Background = Color.White.copy(alpha = 0.05f)

    val OuterBorder = Color.White.copy(alpha = 0.12f)

    val InnerBorder = Color.White.copy(alpha = 0.04f)

    val Accent = Color(0xFF35D5FF).copy(alpha = 0.18f)

    val Radius = 32.dp

    val Stroke = 1.dp

}